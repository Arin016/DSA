#include<bits/stdc++.h>
using namespace std;
#define int long long
int mod = 1e9+7;

//YOU CAN POSSIBLY MAKE A MISTAKE HERE!!! LOOK CAREFULLY!!!!
//tier based Airway system of cities, tier 1 ---A--- tier2 ---B---- tier3 ---C--- tier1
//also a circuit of roadways
//tier1(in) tier2(in) tier3(in)
//tier1(out) tier2(out) tier3(out)
//from cities of tier 1 to tier1(in) 0 wt edge and from tier1(out) to tier1 0 wt edge

void solve(){

}

signed main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t=1;
    cin>>t;
    while(t--){
        solve();
    }
}
