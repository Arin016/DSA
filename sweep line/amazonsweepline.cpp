#include<bits/stdc++.h>
using namespace std;
#define int long long
int mod = 1e9+7;


//all you had to do was sort the processess take prefix sum 
//and then for each processor put a UB and LB in the processor array

void solve(){

}

signed main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t=1;
    cin>>t;
    while(t--){
        solve();
    }
}
